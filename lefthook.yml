pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,tsx}"
      run: bun run lint-check
    typecheck:
      glob: "*.{ts,tsx}"
      run: bun run typecheck
    cloudflare-test:
      glob: "{src,validation}/**/*.{ts,tsx}"
      run: bun run test
