pre-commit:
  parallel: true
  commands:
    biome:
      glob: "*.{js,ts,tsx}"
      run: npm run lint-check
    typecheck:
      glob: "*.{ts,tsx}"
      run: npm run typecheck
    cloudflare-test:
      glob: "{worker,shared,resources/js/cloudflare}/**/*.{ts,tsx}"
      run: npm test
